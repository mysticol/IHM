build.classes.dir=${build.dir}/jar
build.dir=build
build.generated.dir=${build.dir}/generated
com.sun.jbi.routing=true
com.sun.jbi.routing.bc.autoconnect=true
com.sun.jbi.sa.internal.routing=true
dist.dir=dist
dist.jar=${dist.dir}/mockProcessReservation.zip
jbi.content.additional=${reference.mockReservation.dist_se}
jbi.content.component=sun-bpel-engine
jbi.service-assembly.description=Represents the Service Assembly of mockProcessReservation
jbi.service-assembly.id=mockProcessReservation
jbi.service-unit.description=Represents this Service Unit
meta.inf=${source.root}/conf
project.mockReservation=../mockReservation
reference.mockReservation.dist_se=${project.mockReservation}/build/SEDeployment.jar
resource.mockReservation=${project.mockReservation}/setup
source.encoding=UTF-8
source.root=src
src.build.dir=${source.root}/jbiServiceUnits
src.dir=${source.root}/jbiasa
test.dir=test
test.results.dir=${basedir}/test/results
