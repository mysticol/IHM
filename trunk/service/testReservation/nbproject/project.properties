build.classes.dir=${build.dir}/jar
build.dir=build
build.generated.dir=${build.dir}/generated
com.sun.jbi.routing=true
com.sun.jbi.routing.bc.autoconnect=true
com.sun.jbi.sa.internal.routing=true
dist.dir=dist
dist.jar=${dist.dir}/testReservation.zip
jbi.content.additional=${reference.reservRestau.dist_se};${reference.reservVoyage.dist_se};${reference.reservManif.dist_se};${reference.reservHotel.dist_se};${reference.ClientManager.dist_se};${reference.ReservationManager.dist_se}
jbi.content.component=sun-bpel-engine;sun-bpel-engine;sun-bpel-engine;sun-bpel-engine;sun-bpel-engine;sun-bpel-engine
jbi.service-assembly.description=Represents the Service Assembly of testReservation
jbi.service-assembly.id=testReservation
jbi.service-unit.description=Represents this Service Unit
meta.inf=${source.root}/conf
project.ClientManager=../ClientManager
project.ReservationManager=../ReservationManager
project.reservHotel=../reservHotel
project.reservManif=../reservManif
project.reservRestau=../reservRestau
project.reservVoyage=../reservVoyage
reference.ClientManager.dist_se=${project.ClientManager}/build/SEDeployment.jar
reference.ReservationManager.dist_se=${project.ReservationManager}/build/SEDeployment.jar
reference.reservHotel.dist_se=${project.reservHotel}/build/SEDeployment.jar
reference.reservManif.dist_se=${project.reservManif}/build/SEDeployment.jar
reference.reservRestau.dist_se=${project.reservRestau}/build/SEDeployment.jar
reference.reservVoyage.dist_se=${project.reservVoyage}/build/SEDeployment.jar
resource.ClientManager=${project.ClientManager}/setup
resource.mokClientManager=${project.mokClientManager}/setup
resource.mokReservHotel=${project.mokReservHotel}/setup
resource.mokReservManif=${project.mokReservManif}/setup
resource.mokReservRestau=${project.mokReservRestau}/setup
resource.mokReservVoyage=${project.mokReservVoyage}/setup
resource.ReservationManager=${project.ReservationManager}/setup
resource.reservHotel=${project.reservHotel}/setup
resource.reservManif=${project.reservManif}/setup
resource.reservRestau=${project.reservRestau}/setup
resource.reservVoyage=${project.reservVoyage}/setup
source.encoding=UTF-8
source.root=src
src.build.dir=${source.root}/jbiServiceUnits
src.dir=${source.root}/jbiasa
test.dir=test
test.results.dir=${basedir}/test/results
